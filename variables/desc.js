/// <reference types="../../CTAutocomplete" />
/// <reference lib="es2015" />
import { BLUE, RED } from "./colors";

export const COMMAND_DEFAULT = `${BLUE}: ${RED}View this information message.`;
export const COMMAND_TOGGLE = `${BLUE}: ${RED}Toggle the module.`;
export const COMMAND_RELOAD = `${BLUE}: ${RED}Reload the module.`;
export const COMMAND_VERSION = `${BLUE}: ${RED}Get the module's version.`;
export const COMMAND_CREDITS = `${BLUE}: ${RED}View the module's contributors.`;
